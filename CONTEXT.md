# ClassIn TeachBuddy 领域术语

本文件记录项目当前已经约定的领域语言，不是产品规格或技术方案。

## 核心术语

### ClassIn TeachBuddy

正式产品名。面向教师的 AI 教学搭档，持续理解获得授权的教学上下文，组织证据，给出可解释判断，生成可编辑成果，并在教师确认后完成后续教学行动。空间受限界面统一简称 **TeachBuddy**，中文描述统一为 **AI 教学搭档**。
_Avoid_：Work Buddy、教师 WorkBuddy、我的教学助理、ClassInBuddy、WorkBuddy by ClassIn

### WorkBuddy（内部工程标识）

既有 `WorkBuddyRun`、模块目录、路由、存储命名空间和测试标识使用的稳定内部名称。它保证历史任务、URL、Receipt 和本地状态兼容，不是当前产品展示名。
_Avoid_：在用户界面把内部工程标识作为品牌显示、为了展示改名而破坏路由或持久化数据

### 通用教师 TeachBuddy

不要求教师先使用 ClassIn，也能通过教师主动提供的课程、学生、教学目标、资料和偏好，支持教研、备课、课堂教学指导和课后服务的完整产品形态。它是 TeachBuddy 的终局产品边界，不等于第一阶段一次性交付全部能力。

### 教学结果

教师在一个明确教学场景中需要完成的可观察成果，例如一节课后的班级诊断、分层学习行动和后续沟通，而不是一次聊天或一次内容生成。

### 教学业务时刻

教师或学生为了推进教学循环而需要完成的一段有明确目标、输入和业务产物的工作，例如课程设计、备课演练、课堂参与、作业订正或课后干预。页面只是承载位置，不是业务时刻本身。

### 教师教学循环

教师从目标与课程设计出发，经过资源生产、备课、课堂、作业、诊断、干预和沟通，再通过教学反思进入下一轮设计的连续循环。

### 完整业务闭环

从一个教学业务目标出发，经过事实与证据、AI 产物或判断、教师确认、业务写回和结果复查，形成可观察业务结果的连续过程。
_Avoid_：功能集合、模型调用链、只有生成没有复查的流程

### 单点 AI 能力

围绕一个确定任务完成一次生成、识别、转换、检索、解释或检查的方法能力，输出由教师或后续流程继续处理。
_Avoid_：业务闭环、Agent

### 组合 AI 能力

多个 Skill、业务工具和规则校验围绕一个业务对象协作，形成可编辑教学产物或待确认判断的能力组合。
_Avoid_：多个模型调用、完整业务闭环

### 课程目标到课程对象

把教学目标、班级约束和课程标准转化为可审阅、可保存的课程、单元和活动对象草稿的课程生产基础链。它是产品业务场景名称，`EDU-01` 只作为内部工程追踪 ID。
_Avoid_：EDU-01 场景、课程生成工具

### 可审教课程方案包

由“课程方案包生成任务”产出的多 Artifact 集合，可包含目标与成功标准、课件、作业、测验、录播/视频、教学资料、活动说明、来源依据和审教记录。它不是 ClassIn 正式课程对象；其中每项只有在教师确认并完成领域写回后，才产生业务系统中的真实变化。
_Avoid_：正式课程、已发布课程、单一课件文件、自动发布的课程对象

### PPT 课件 Artifact

可由“单课件生成任务”独立产出，也可作为课程方案包中的一项产物；包含页面结构、页面内容以及后续审阅、修改与版本。它拥有独立的来源、状态和交付结果，不因属于方案包而失去独立生命周期。
_Avoid_：课程方案包、课程对象、自动发布的 PPT

### ClassIn 空间

由 ClassIn 拥有的个人、组织和业务文件存储载体，负责文件引用、存储权限和可访问状态。它可以承载 TeachBuddy 产物文件，但不拥有 Agent Run、Artifact 语义或 TeacherIn 的作品发布状态。
_Avoid_：WorkBuddy 文件系统、TeacherIn 作品库、会话历史

### 我的文件

WorkBuddy 对 ClassIn 空间中历史 Agent Run 产物的组织与检索视图，只收录教师与 AI 协作生成的 Artifact 及其文件结果，并保留产物与来源会话、Run、版本及下游动作回执的关联，便于作为新上下文、继续编辑、发起教学动作或定位 TeacherIn 作品。它不提供上传入口，不收录教师上传、组织共享或仅作为会话输入的文件，也不建立独立存储空间或复制 ClassIn 的文件所有权。
_Avoid_：第二套云盘、WorkBuddy 私有文件系统、文件上传入口、会话输入资料库、TeacherIn 内容广场

### TeacherIn

ClassIn 体系内保持独立品牌的教研内容生产与市场分发产品线，拥有作品管理、发布、授权、定价、获取和衍生规则。原“资源中心”已升级并合并到 TeacherIn，不再视为另一套资源存储空间。
_Avoid_：资源中心与 TeacherIn 并列的两套内容库、WorkBuddy 内容广场

### TeacherIn 作品

进入 TeacherIn 内容生命周期、可继续编辑并按规则发布、授权和分发的教研内容对象。WorkBuddy Artifact 只有经教师明确执行“创建草稿到 TeacherIn”后才成为作品草稿，并保留来源 Run、版本和创作方式。
_Avoid_：任意会话附件、自动发布的 AI 产物、ClassIn 云盘文件

### TeacherIn 作品草稿

由教师选择的 WorkBuddy Artifact 或 TeacherIn 人工创作内容形成、尚未发布的作品状态。它可以在 TeacherIn 中继续编辑并定位回来源 Artifact；创建成功只表示已创建草稿，不表示已发布。
_Avoid_：已发布作品、自动上架内容、WorkBuddy Artifact 的重复文件

### TeacherIn 兼容内容包

由独立 TeachBuddy 或 ClassIn 内部生产、在内容类型、结构、素材、学段学科、版本来源、授权和生命周期语义上完全遵循 TeacherIn 内容契约的教学内容包。兼容表示连接后无需重新制作或转换内容格式即可进入 TeacherIn 内容生命周期，但不表示两个产品共享账号、路由、数据库、权限或自动同步。
_Avoid_：独立格式、跨产品共享数据、已自动发布的 TeacherIn 作品

### 内容生态兼容

独立 TeachBuddy 与 TeacherIn 使用同一套内容生产格式和内容契约，使外部教师生产的内容具备进入 ClassIn 内部生产、编辑、授权和分发闭环的条件。它是格式与语义层兼容，不是未授权的运行时互通。
_Avoid_：页面跳转互通、数据库直连、默认同步、格式近似兼容

### 创建草稿到 TeacherIn

教师把选定的 WorkBuddy Artifact 转换为 TeacherIn 作品草稿的显式动作。成功反馈统一提示“你可以前往 TeacherIn 继续编辑作品信息、设置授权并发布。”；该动作不等于发布。
_Avoid_：沉淀到 TeacherIn、发布到 TeacherIn、提交审核

### 单课件生成任务

以制作一份可预览、可编辑、可保存的课件为目标的独立 Agent Run。它可以不依赖课程方案包完成，也可以在完成后作为来源创建关联的课程方案包任务。
_Avoid_：课程方案包的隐藏步骤、只返回大纲的聊天、自动发布任务

### 课程方案包生成任务

以教学目标为起点，规划并生成一组相互关联教学产物的独立 Agent Run。产物可逐项审阅、修改、排除和写回，部分失败不抹掉其他已完成结果。
_Avoid_：单课件任务的新版名称、ClassIn 正式课程对象、不可拆分的批处理

### 关联任务

由已有 Run 或 Artifact 发起、保留来源引用但拥有独立目标、Context Snapshot 和生命周期的新 WorkBuddyRun。关联关系用于导航与追溯，不默认形成父任务对另一个任务的生命周期控制。
_Avoid_：原任务改名、原任务被吞并、共享一个可变上下文的子流程

### 审教意见

教师针对课程方案或教学产物提出的专业审阅记录，包含定位、问题类型、严重程度、意见内容、处理方式和处理状态。审教意见可以驱动人工修改或 AI 定向修订，但不能被模型自动视为已解决。
_Avoid_：普通文本校对、模型反馈、未确认建议

### 最终确认稿

教师完成审教并明确确认、可以进入保存或后续业务动作的 TeachBuddy 产物版本。最终确认稿不等于 ClassIn 的正式发布稿，正式发布仍由领域规则和业务流程决定。
_Avoid_：生成完成、自动发布稿

### 学生学习循环

学生从理解目标和计划开始，经过课堂参与、练习提交、求助反馈、订正掌握和自主学习，再根据成长证据调整下一目标的连续循环。

### 教学产物

教师或学生在一个教学业务时刻中形成的可保存、可编辑、可交付对象，例如课程目标、教案、课件、作业、评语、干预计划、订正记录和反思。AI 输出只有进入业务产物或待确认动作，才真正进入教学流程。

### 学习任务

学生需要完成的正式学习行动及其状态。教师布置的作业或活动、自主学习活动和沟通中的行动项必须分开，不因 AI 建议自动改变完成状态。

### 学习证据

由课堂参与、作答、提交、反馈、订正和自主练习产生的可追溯事实。学习证据可以支持解释和诊断，但不能把模型推断直接固化为学生标签。

### 教学上下文

教师工作现场中与当前任务有关信息的总称。需要讨论所有权和治理时，应进一步明确为业务上下文、Domain Knowledge、教师输入、教学证据或 AI 推断，不能把它们混成同一种事实。
_Avoid_：把所有可访问数据都发送给模型、无来源的混合记忆、无权限的学生画像

### 核心上下文（Core Context）

一次 WorkBuddyRun 可查看、可调整并在执行前冻结的受治理上下文集合。它组织业务上下文、Domain Knowledge、教师资料和任务约束，但不复制或篡改这些信息的事实所有权。
_Avoid_：调用上下文、长期无治理记忆、附件列表、完整业务数据库副本

### 业务上下文（Business Context）

由 ClassIn 拥有的身份、机构、班级、课程、单元、活动、成员、资源、时间和业务证据引用。WorkBuddy 只能按当前 Actor、Tenant 和权限读取并引用，不能成为这些事实的第二所有者。
_Avoid_：Domain Knowledge、AI 推断、WorkBuddy 私有业务副本

### Domain Knowledge

课程标准、学科知识、教学法、机构规则、审教规则、模板和评价量规等受版本与适用范围治理的知识。它与班级、学生和作业等实时业务事实分区表达。
_Avoid_：学生事实、未确认的 AI 推断、无版本的 Prompt 集合

### 上下文来源（Context Source）

一个 Context Item 的事实所有者、对象 ID、版本或更新时间、权限范围和进入当前 Run 的入口。来源使教师和系统能够判断它是实时事实、历史引用、教师输入还是 AI 推断。
_Avoid_：只显示值不显示出处、把推断标成 ClassIn 事实

### 上下文提案（Context Proposal）

系统根据当前入口、任务意图和教师权限自动建议、但尚未由教师确认的上下文集合。跨组织或改变主班级时必须重建提案，不能静默沿用不兼容数据。
_Avoid_：已冻结快照、自动确认、最近使用即默认带入

### 上下文投影（Context Projection）

某个 Skill 或 Tool 在一个具体步骤中，依据 Capability Manifest、使用目的、教师选择和权限从 Context Snapshot 获得的最小必要参数子集。
_Avoid_：完整上下文快照、所有学生数据、未下发字段的执行日志

### 上下文增强

教师使用 ClassIn 后，WorkBuddy 在获得授权的前提下接入更连续的课堂、作业、互动、组织和执行数据，使同一套通用能力产出更懂教师、更精细、更专业和更个性化的结果。ClassIn 是增强层，不是 WorkBuddy 唯一可用的前提。

### 教学证据

可以回溯到课堂、作业、互动、资源或反馈来源的事实。证据与模型推断必须分开表达。

### 组织成员身份

同一用户在某一机构中的角色、职责和权限范围。教师跨机构工作时拥有多个相互隔离的组织成员身份，不能将一个机构的业务上下文自动带入另一个机构。

### 班级课程

一个班级中围绕明确教学目标和生命周期组织的课程实例。它连接班级、目标、单元、教学活动、课堂和作业，不等同于长期班级关系或通用课程模板。

### 教学目标

教师希望特定学习者在一定范围和时间内达到的学习结果。教学目标约束课程设计、教学活动、评价、诊断和后续干预。

### 成功标准

用于判断教学目标是否达到的可观察条件。成功标准是评价和复查的依据，不等同于单一分数或模型置信度。

### 诊断假设

基于教学目标和学习证据，对学习问题、可能原因或趋势形成的暂定解释。它属于 AI 或人的推断，不是学生事实，必须允许教师确认、修正、否定和使其过期。

### 教师确认结论

教师对诊断假设作出专业审阅后形成的业务判断。它可以成为干预行动的依据，但仍应保留来源、适用范围和确认时间。

### 干预计划

围绕已确认的教学问题组织的目标、行动、资源、沟通与复查安排。它通过作业、待办、资源和消息等既有业务对象执行，不形成平行任务状态。

### 复查结果

在约定节点用于判断教学行动是否完成、是否有效以及是否需要调整的后续证据。它将干预重新连接到诊断、反思和下一轮教学目标。

### 事实所有权

每类业务事实由一个明确领域负责最终状态和语义。WorkBuddy 可以读取、解释和编排，但不能复制课程、课堂、作业、消息、待办、成绩或权限形成第二套事实。

### 课后教学包

课后诊断与干预闭环中的候选阶段成果：由 WorkBuddy 汇总课堂证据，生成班级与学生差异判断、下一步教学建议、分层学习行动和沟通草稿，经教师确认后执行并追踪。它是优先验证候选，不代表已经确定为第一条实施切片。

### 课后诊断与干预闭环

从已确认的课堂或作业证据出发，形成诊断假设、个性化干预计划和待确认动作，并通过作业、待办、资源或消息执行，最后在复查节点读取结果并调整下一步的连续过程。课后教学包是这个闭环中的阶段性成果。

### AI 工具

围绕一个确定任务完成生成、识别、转换、检索或解释的单点能力。它不跨业务对象规划和执行。

### AI Copilot

理解当前角色、业务对象、权限和证据，在现有工作流中生成建议、解释或可编辑草稿的协作模式；关键结果由用户确认后才写回业务对象。Copilot 不是主 Agent 调用的一种底层协议。

### AI Agent

围绕用户目标装配上下文、制定可检查计划、调用白名单业务工具、处理失败，并在审批边界内执行和跟踪结果的能力。聊天界面、Agent 名称或长期对话本身不构成 Agent。

### 有限 Agent

仅在明确目标、权限、工具和审批范围内自主规划与执行的 AI Agent。它不拥有开放式教育决策权，高风险判断和不可逆行动仍由教师控制。

### 统一教师 WorkBuddy 工作台

教师 WorkBuddy 的终局产品壳层，以对话协调目标，以结构化工作区承载计划、教学产物、上下文、证据、审批和复查。情境入口与独立入口共享同一个 WorkBuddy 身份和任务状态。
_Avoid_：万能聊天框、Agent 列表、多个割裂 AI 应用

### 教师 WorkBuddy 主 Agent

教师在统一工作台中面对的唯一 WorkBuddy 身份，负责理解目标、组织计划、选择能力、协调审批并汇总最终结果。它可以委派专业子 Agent，但不把底层协作复杂度暴露为教师的选择负担。
_Avoid_：总控 Agent、Agent 市场入口

### 专业子 Agent

接受主 Agent 委派，围绕独立目标、明确完成契约和受限上下文运行的专业 Agent。只有需要独立权限、生命周期、失败恢复或审计时才成立，简单生成、检索和业务写入仍属于 Skill 或工具。
_Avoid_：把每个 AI 功能都命名为 Agent

### A2A 委派

主 Agent 与专业子 Agent 之间基于目标、上下文引用、权限、完成契约和结果回执进行的任务委派。它不等同于在界面中提供多个智能体供用户选择。
_Avoid_：多 Agent 展示、Agent 应用市场

### 任务意图

教师目标、业务范围、约束和成功标准的结构化表达。任务意图说明 WorkBuddy 要协助完成什么，不等同于具体执行计划。
_Avoid_：Prompt、聊天消息

### Agent Run

WorkBuddy 围绕一个任务意图形成的持久运行记录，包含计划、状态、等待项、审批、失败和复查节点。它拥有 Agent 任务状态，不拥有课程、作业、消息或待办等业务状态。
_Avoid_：对话 Session、业务任务副本

### 上下文快照

某个 Agent Run 在一个时间点实际使用的 Context Item 引用、规则版本、时间点和权限范围的冻结记录。它用于解释历史判断，不替代 ClassIn 的实时业务事实，也不直接等于任何单次 Skill/Tool 的调用参数。
_Avoid_：实时业务副本、长期记忆、完整调用参数、无来源的 Prompt 拼接

### 待确认动作

WorkBuddy 基于计划提出、尚未成为业务事实的创建、修改、发布或发送建议。它必须声明目标对象、参数、差异、风险和可逆性，并在权限和审批通过后才能执行。
_Avoid_：已执行任务、业务事实

### 执行回执

业务事实所有者在执行待确认动作后返回的结构化结果，记录实际对象、结果、时间、失败和可撤销信息。它是 Agent 判断执行状态的依据，不替代业务对象本身。
_Avoid_：模型自述成功、聊天确认文案

### 业务工具

通过对应事实所有者的权限、校验和状态机读取或写入真实业务对象的受控能力。业务工具必须声明副作用、幂等、可逆性和审计要求。
_Avoid_：直接数据库写入、无权限函数调用

### WorkBuddy 终局能力地图

描述终局产品要帮助教师完成哪些教学结果、具备哪些能力域，以及教师如何输入目标、获得成果、确认行动和理解反馈。它回答“产品做什么、呈现什么体验”，不负责描述底层业务对象或分阶段实现顺序。

### 核心对象与上下文关系图

描述教师、组织、班级、课程、单元、活动、学生、目标、教学产物、任务、证据、干预和结果之间的业务关系、时间范围与权限边界。它回答“WorkBuddy 正在理解和操作什么”，不是页面或功能模块之间的导航关系图。

### AI 自主性与执行成熟度路线图

描述同一项业务能力如何从单点 AI 工具，升级为理解上下文并生成可确认草稿的 Copilot，再在具备计划、工具、审批、失败恢复和结果跟踪后成为 Agent。它回答“AI 以多大自主权怎么做、何时可以升级”；并非所有能力最终都必须升级为 Agent。

### 智能执行层

WorkBuddy 在 ClassIn 教学业务系统之上的产品角色：把分散的事实、判断、成果和授权行动串成闭环，而不是另一个聊天入口或智能体目录。

### 教师确认

教师对 AI 生成的判断、成果或行动拥有可见、可编辑、可拒绝和可撤销的控制权。高风险教学评价默认停留在确认后执行。

### 愿景 Demo

用于战略对齐和表达终局体验的展示轨道，可以使用模拟数据或未上线能力；它不能作为真实需求或产品价值已经成立的证据。

### 验证型产品

使用真实教师、真实课堂、真实权限和真实执行结果验证一个窄场景闭环的产品轨道。它决定是否扩大投入。

### 独立产品

脱离 ClassIn 既有入口、组织关系和历史数据后仍能完成核心任务的产品形态。独立不排斥采用与 ClassIn/TeacherIn 相同的内容契约；产品运行、账号和数据继续隔离，内容格式可以完全兼容。它是通用教师 WorkBuddy 的终局形态；第一阶段可以先以内嵌 ClassIn 的场景验证核心闭环，再逐步开放独立使用、资料导入和外部连接器。

### 演示真值标记

在愿景 Demo 中标明一项数据、能力或回执属于真实、模拟、集成模拟还是未来能力的状态说明。它用于防止把演示完整性误认为生产能力、数据质量或业务价值已经成立。
_Avoid_：默认 Demo 中可见的能力都已经上线

### 验证型纵向切片

围绕一个明确教师结果，贯通真实身份、上下文、业务对象、AI 产出、教师控制、执行和复查的最小可验证闭环。它必须能用真实用户、权限、数据和结果决定继续、扩大、转向或停止。
_Avoid_：孤立 AI 功能、只有生成没有结果的 Demo

### 影子模式

AI 在真实授权范围内生成判断或行动建议，但结果不对学生或外部对象生效，也不触发业务写入，仅供教师或专家离线对照评价的验证方式。
_Avoid_：无提示地影响正式结果、把影子结果写入学生事实

### 决策门

阶段升级前必须满足的一组价值、质量、数据、控制、安全、成本和业务证据条件。通过决策门只授权扩大一个明确范围，不代表后续阶段自动获批。
_Avoid_：按日期或功能完成度自动升级
