-- Replace placeholders with numeric keys returned by sample selectors in this run.
SELECT activity_id,biz_type,action,COUNT(1) AS event_n,COUNT(DISTINCT uid) AS learner_n,MIN(event_time) AS first_event,MAX(event_time) AS last_event FROM eo_pdviews.dwd_acitive_user_lms_action_details_di_view WHERE dt>='2026-09-05' AND dt<='2026-09-11' AND event_time>=1788537600 AND event_time<1789142400 AND role='student' AND action IN ('handin_homework','handin_exam','looked_activity') AND activity_id IN ({{sample_activity_ids}}) GROUP BY activity_id,biz_type,action ORDER BY activity_id,biz_type,action LIMIT 60;
