# 本机迁移恢复验收（2026-09-10）

范围：从相邻 classin-ai-harness 目录恢复 v2 的本机配置与运行数据；页面和业务代码未修改。

根因：旧目录 Harness 占用 3080，新目录 Harness 启动报 EADDRINUSE；BFF 校验 host.cwd 后拒绝旧项目。迁移未包含 Git 忽略的 .env 与 .runtime，导致模型凭据及历史数据缺失。

处理：停止旧目录占用 3080 的 Harness；将新目录原运行状态备份到 .scratch/runtime-before-migration；仅在本机复制 .env（权限 0600）及会话、文件、产物、保存结果、上下文与 Harness 持久数据。原目录保留。Harness 历史日志保留原目录编码，不能直接改名为新工作目录：压缩日志 header 的 cwd 必须与编码目录一致。新会话由新工作目录创建。

验收：
- GET /api/teachbuddy/health 从 offline 恢复 ready。
- 本地上下文接口返回 200；迁移后 ideal-full 会话列表包含 59 条历史记录。
- 经产品 BFF 新建会话并执行真实模型连接测试，模型返回“连接正常。”，最终 idle。
- 浏览器进入教师消息 / 高二物理 3 班，历史助手对话与 10 项建议可见；课后 Tab 可切换，同步任务按钮和助手输入框 enabled，离线错误消失。

限制：未逐条验收所有历史附件与旧会话继续执行；浏览器原有 localStorage 不属于目录复制，本次未做跨浏览器存储迁移。固定端口设计仍要求旧、新目录的 Harness 不同时运行。此修复为本机迁移，无源码行为变更，使用健康检查、实际模型请求和浏览器验收，不新增实现镜像测试。凭据及本地上下文均保持 Git 忽略，不写入本记录。
