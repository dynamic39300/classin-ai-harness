---
title: TeachBuddy 教学阶段导航方案（二）
status: IMPLEMENTED_PENDING_USER_REVIEW
version: v0.1
branch: codex/copilot-stage-nav-v2
base_branch: codex/copilot-prompt-grid-v1
updated: 2026-09-10
---

# TeachBuddy 教学阶段导航方案（二）

## 1. 方案目标

在 TeachBuddy 顶部提供一个稳定、明显的教学协作入口。老师通过`课前 / 课中 / 课后 / 总结`四个阶段快速理解当前班级有哪些课程、学生和事项，并用一次点击把明确要求提交给 AI。

本方案与方案一复用同一个`TeachingDynamicsAdapter`、业务事实、Prompt 动作和对话闭环，只替换顶部信息组织和本地呈现状态，便于对比评审。

## 2. 页面结构

```text
教学动态                                      [暂停/继续] [收起]

  ●────────●────────●────────●
  课前      课中      课后      总结
  建议1条   建议1条   建议2条   建议2条

┌ 课中                                      当前阶段 ┐
│ 当前班级的课程、学生与任务                         │
├──────────────────────────────────────────────────┤
│ 高二物理3班 · 动量守恒模型 · 在线课堂              │
│ 已开课10分钟，3人迟到                  [提醒学生]   │
│ 李明、周然、陈晨尚未进入课堂                       │
├──────────────────────────────────────────────────┤
│ 高二物理3班 · 动量守恒模型 · 在线课堂              │
│ 全班已完成2次随堂互动                  [已了解]     │
└──────────────────────────────────────────────────┘
```

顶部固定四个 Tab。圆点和连线表达教学阶段位置，不表达同一任务的完成流程。当前业务阶段以绿色轮廓标识，当前选中阶段以绿色实心圆标识。

## 3. 内容规则

每个阶段卡片显示当前班级中属于该阶段的全部有效事项。不同课程可以同时落在不同阶段，例如动量守恒课正在进行，电磁感应课仍处于课前准备，上一讲作业处于催交与错题分析阶段。

每项依次表达：

1. 班级、课程和业务载体；
2. 当前人数、截止时间或完成事实；
3. 已知学生范围和必要边界；
4. 一个明确动作。

推荐动作使用教师语言：`提醒预习`、`提醒学生`、`提醒交作业`、`查看错题`、`制作解析`、`生成总结`。点击后直接进入现有 TeachBuddy 对话；缺少信息时由 AI 补问。

## 4. 切换、轮播与收起

- 首次打开默认选中当前业务阶段。
- 自动切换每 8 秒进入下一个阶段。
- 鼠标进入或键盘焦点位于模块内时暂时停止计时。
- 老师手动切换 Tab 后停止自动切换，右上角播放按钮可以重新开启。
- 系统尊重`prefers-reduced-motion`，减少动态偏好的用户不自动轮播。
- 键盘支持左右方向键、Home 和 End 切换 Tab。
- 收起后常驻为`教学动态｜N 项建议`一行；业务更新只刷新摘要和`有更新`提示，不强制展开。

## 5. 业务与 AI 状态边界

Tab 数字只统计当前业务阶段内可以发起的建议，不表示 AI 任务数量。卡片随课程开始、结束、作业截止和正式回执自然更新。AI 的补问、生成、修改、审阅、发送和失败恢复全部保留在下方对话中。

## 6. 验收重点

- 四个 Tab、圆点和连线在 Sidecar 默认宽度下完整可见；
- 每个 Tab 的`建议 X 条 / 已核对 / 待核对`可直接理解；
- 切换阶段只替换一张内容卡，不跳页、不出现返回；
- 手动切换、自动切换、暂停、收起与恢复互不改变业务事实；
- 事项点击一次进入 AI 对话，无表单和二次提交；
- 内容卡在 900×720 与 1440×900 下无横向溢出；
- 相关组件、领域投影、Adapter、Sidecar 与 Chromium 闭环测试通过。

## 7. 研究依据

交互依据与可访问性边界见[教学阶段导航研究](../../../01-research/IM-COPILOT-STAGE-NAVIGATION-RESEARCH.md)。
