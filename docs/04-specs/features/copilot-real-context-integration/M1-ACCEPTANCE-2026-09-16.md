---
title: M1 已预验证十题真实接入实施及验收记录
status: COMPLETE_USER_ACCEPTED
milestone: M1
date: 2026-09-16
---

# 结论

M1 已完成实现、真实环境联调、DeepSeek 十题运行和浏览器回归，并于 2026-09-16 通过用户验收。M2 已解锁。

真实测试 Thread 的“可以问什么”现按当前能力开放 A1、A2、A3、A4、B1 课后、C1、C2、E1、E2、F1 捕获试点十题。每次请求先按问题和已选择对象形成白名单 Route Receipt，再通过现有同源 BFF 读取真实测试接口；身份、URL、上游参数和分页不能由模型覆盖。开放式输入继续可用，缺少活动对象时沿用现有补查/澄清链路。

# 十题真实运行结果

| 题 | 实际工具 | 本次真实样本结果 | DeepSeek结果 |
| --- | --- | --- | --- |
| A1 | `read_course_progress` | 读取班级下2门课程；数学专题课程15单元、54项活动、15节课堂、当前已结束3节；另一课程单独显示0活动 | 通过；各课程分列，没有混算百分比 |
| A2 | `list_course_activities` | 下一课为第3讲“压轴题：数轴上的动点问题”，2026-09-18 15:30–17:30，星期五 | 通过；日期、星期和后续课节顺序一致 |
| A3 | `read_class_roster` | 学生3人；identity=1单独计数，旁听/教师/助教/其他身份分列 | 通过；回答学生3人，不把其他身份计入 |
| A4 | `read_lesson_companions` | 以第2讲真实单元ID关联已发布活动，不按名称跨单元猜测 | 通过；类型、名称和时间来自真实活动目录 |
| B1 | `read_class_attendance` | 第2讲正式课后报告：应到3、实到2、迟到0、报告时长1284秒 | 通过；明确是课后数据，不冒充实时在线名单 |
| C1 | `read_activity` | 第2讲单元练习的真实说明、截止、分配和资源元数据 | 通过；未取得的图片正文不作推断 |
| C2 | `read_homework_students` | 分配3人，已提交/已批阅1人，未提交2人；姓名由同班成员接口映射 | 通过；未交、待批和已批口径分开 |
| E1 | `read_class_resources` | 报告、回放文件、板书/高光和教师笔记分别投影 | 通过；存在性、数量和正文可读性分开 |
| E2 | `read_class_resources`、`read_class_transcript`、`read_ai_analysis` | 第2讲2个回放文件、原始ASR、3条教师笔记和AI授课分析；原始转写与AI生成内容分源 | 通过；课堂回顾基于真实内容，不把AI分析当正式学习结果 |
| F1 | `read_im_history` | 读取保存的20条未遮蔽班群原文；请求最近5天，实际消息范围为2026-09-16 14:43:42–15:00:45（Asia/Shanghai），快照不代表完整分页 | 通过；首次运行发现UTC显示错误，修复本地时间投影后重跑通过 |

真实页面复核时，A1曾有一次把正确的绝对日期2026-09-18改写成错误的相对词“明天”。回答合同现已禁止“今天/明天/后天”等相对日期，只允许绝对日期、星期和时间；修复后A1再次运行不再出现该冲突。

受限运行明细保存在忽略目录 `.runtime/private/classin-test/m1-context-check.json` 和 `m1-deepseek-check.json`，不提交真实聊天正文或学生私有数据。

# 实现范围

| Ticket | 结果 |
| --- | --- |
| RC-100 | 十题绑定最小白名单工具和对象要求；Context携带计划/实际工具、对象、版本和时间 |
| RC-101 | 新增班级全课程读取，逐课程核验单元、活动、已结束和下一课堂 |
| RC-102 | 下一课按可信时钟、发布和取消状态过滤；成员接口扩展为1/2/3/4身份分列 |
| RC-103 | 本讲配套内容仅按相同`unitId`关联 |
| RC-104 | 已结束课堂读取正式课后报告；未来/课中仍不回答实时出勤 |
| RC-105 | 作业要求、完整分配名单、同班姓名、提交/草稿/批阅状态沿用真实详情链 |
| RC-106 | 报告、回放、板书/高光、笔记按证据种类分层 |
| RC-107 | 新增回放文件→ASR及AI授课分析读取；逐文件保留失败，限制模型输入容量并说明截取 |
| RC-108 | 新增受限本机真实消息快照Reader；原文不脱敏进入模型，显示查询窗、实际范围、条数与不完整性 |
| RC-109 | 十题真实Context→DeepSeek全部完成；帮助入口显示十题；历史、发送、错误恢复回归通过 |

# 验证

- `npm run test`：156个测试文件、1011项全部通过；
- `npm run typecheck`、`npm run lint -- --quiet`、`npm run build`、`git diff --check`：通过；构建仍有既有主Chunk体积提示；
- `classin-messages-hybrid.spec.ts`：7项通过；
- 真实同源Context：十题全部HTTP 200并带Route Receipt；
- 真实DeepSeek：十题全部生成回答，F1时区修复后复跑通过；
- 真实浏览器：帮助入口显示`A1 A2 A3 A4 B1 C1 C2 E1 E2 F1`十题，A1真实答案可见；截图为本机临时证据`/tmp/m1-real-demo-browser-smoke-2026-09-16.png`。

# 本次请用户验收

入口：`http://127.0.0.1:4175/teacher/messages?category=class&thread=classin-test%3Aclass%3A591820%3Acourse%3A3153610`

1. 打开“可以问什么”，确认五组中共十题，名称和对象符合当前测试班；
2. 重点抽查 A1、A4、B1、C2、E2、F1 的问答匹配、相关性和回答质量；
3. F1应明确20条及实际北京时间范围，不宣称取得完整五天历史；
4. E2应区分原始转写、教师笔记和AI生成分析，不评价学生掌握；
5. 若十题整体可接受，确认M1通过；之后才进入M2实时出勤与参与。

# 保留边界

- F1仍是已授权真实捕获快照，尚非独立实时IM Reader；稳定化属于M5；
- B1本里程碑只完成课后分支，成员级实时出勤属于M2；
- 普通IM写接口仍未接通，消息发送继续是老师端本机模拟传输回执；
- 十个固定问题使用已验证的确定性路由；自由输入由同一Context入口和Runtime补查处理。页面不要求老师审阅工具全集，用户验收仍聚焦问答效果。
