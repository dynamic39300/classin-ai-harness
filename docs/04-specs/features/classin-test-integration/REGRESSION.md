# 2026-09-15 整体回归清单

**当前状态：D-156 校准目标已完成。** 原系统框架保留，真实课程进入原 Copilot，老师端模拟发送闭环已验证。

## 当前验收：D-156 原工作区与模拟补齐

用户本次校准优先于下方的上一阶段真实 IM 验收门槛。当前交付以“原完整 UI + 真实课程读取 + 明确模拟的未接入能力 + 老师端模拟发送”验收；真实 IM/学生端送达留为后续接入任务，不用模拟结果证明其已完成。

主入口：[原消息工作区](http://127.0.0.1:4174/teacher/messages?category=class&thread=class-physics-3)。点击顶部“测试班级 · 模拟消息”，进入真实测试班的同版工作区。退出沉浸模式后，首页、TeachBuddy、班课管理、课程表、待办、教学洞察、空间、消息、黑板、投屏等原导航继续保留；原演示班继续按原 Mock 服务运行。

数据核验入口：[真实课程和活动明细](http://127.0.0.1:4174/teacher/classin-test)，用于核对 API，不再作为唯一工作页面。可从消息区“查看课程数据”前往，并从核验页“进入班级消息与 Copilot”返回。

| 检查项 | 当前实现 / 验证证据 | 用户反馈 |
| --- | --- | --- |
| 原系统框架、消息四分类、会话列表、时间线、输入框、右侧 Copilot | 沿用原组件；物理演示班保留原消息、教学动态、回放卡和临时教室；真实测试班独立 Thread；1440×1000 浏览器无溢出或 pageerror | |
| 真实课程读取 | 本轮真实 API 回读 54 项活动 / 3 学生 / complete=true；查询、资源、详情沿用已实测 BFF | |
| 真实课前、作业/测验提醒 | 同一消息页面显示真实课堂日期与任务截止时间；真实模型 10.437 秒生成 9月15日19:30–19:45 有理数短课提醒，次日19:30作业/测验截止也与已读取数据一致 | |
| 老师手动发送 | 当前测试 Thread 可发送文本和图片，显示“已发送 SIMULATED”；仅本浏览器老师端记录 | |
| Copilot 审阅、编辑、取消、确认 | 审阅可编辑；取消没有新增消息；确认后出现老师消息与明确模拟回执；未调用真实 IM 发送接口 | |
| 刷新与重复发送 | 同一浏览器标签页刷新后文字、图片与发送状态恢复；相同 artifact/version 重试不重复发送；改变同一幂等请求内容拒绝；存储失败保留可重试状态 | |
| 学生端隔离 | 测试 Thread 只对老师可见；学生角色提交及历史读取被拒绝，页面不显示这些消息；没有“送达/已读/人数”回执 | |
| 未接入的课中能力 | “模拟到课提醒”使用独立虚构学员甲/乙/丙；真实模型 8.605 秒生成带模拟标识的示例，取消后消息数量不变；不覆盖实际实到0等数据 | |
| 未接入的课前/课后/总结能力 | 四阶段共6个标记入口：模拟改期通知、到课提醒、测验批阅、个性化回顾、授课分析、媒体学习总结。生成的是主题相关模拟示例，不写入真实课表、成绩或分析记录 | |
| 来源和错误 | 默认问题读取真实数据；明确请求模拟才注入 fixed-demo 来源及标签。真实API失败不改用虚构事实，输入保留、可重试；真实0/空/未提交保持原值 | |
| 群资料与班级入口 | 展示实际班级名册，并注明不是实际IM关系；不猜班主任/群号；班级入口可返回真实数据核验 | |
| 会话恢复 | 异步连接/刷新不重建 Runtime；原两项过期绑定恢复 E2E 保留“只创建一次”断言并通过 | |

本机模拟历史使用 sessionStorage，按 teacher/tenant/thread 隔离；支持同一标签页刷新，不承诺关闭标签页后或跨设备恢复。图片消息使用本机 data URI；浏览器容量不足时明确失败，未记成功。真实学生客户端、本机以外的消息同步、正式改期/批阅写回、完整媒体识别/视频转写、真实AI分析全文仍未接入。六个模拟入口供流程和文案体验，不把生成示例当成正式业务执行回执。

私有实测证据：`.runtime/private/classin-d156/live-verification.json`、`verified-draft.txt`、`simulated-attendance-draft.txt`、`live-sent.png`、`full-framework.png`。本轮没有新增/改动 ClassIn 教学业务记录。

验证过程还修复了两处回归：混合来源标记导致原物理场景超出其3,800字符上下文预算（实际3833），已只对混合来源添加逐项 kind；异步班级连接导致 Runtime 实例重建及重复创建恢复会话，已固定组合根实例。失败证据保留，未放宽原断言或超时。

自动化结果：全量 **148文件 / 979项 Vitest**、**33项 Playwright** 通过；类型检查和 ESLint 无错误/警告，构建通过（保留既有的大 bundle 提示）。角色切换与同 Thread 不同身份隔离收尾后，再次通过8项相关单元测试、4项混合工作区 E2E及类型/lint/构建。最后的接入测试还验证 HTTP 缺少 randomUUID 时两次发送跨刷新不重号。

日志：`.runtime/private/classin-d156/check-accepted.log`、`playwright-final.log`、`isolation-final.log`、`hybrid-final.log`、`typecheck-final.log`、`lint-final.log`、`build-final.log`。原回放/PDF/题图的29项浏览器用例均继续通过；未重新造数或批量写入。

## 2026-09-16 学生提交与教师批改现场补证

用户使用自有测试教师和两个测试学生账号完成真实操作，采集前已同时监听教师 `7777`、学生 `7779` 和另一学生 `7780`。本轮验证两名学生完成第一讲测验交卷，其中第二名学生在教师批改后收到“测验被批阅-无系统评分”事件，并回读 `status=2/isStuScore=1/showScore=25`；第一名学生完成第二讲作业后，教师与学生两端均回读到提交和教师操作后的状态变化。

测验子窗口的原生日志捕获 `POST /api/exam.api.php?action=submitPaper`：12 次进行中保存使用 `isExamEnd=2`，最后一次正式交卷使用 `status=3/isExamEnd=1/isAuto=0`，携带10题答案集合；约两秒后的教师/学生回读确认提交成功。直接响应外壳没有留存，教师 `reviewPapers` 发起请求、作业 PC 写路径及其 ACK/幂等/失败恢复仍待补。CEF CDP 只看到了 `studentUnitActivityList/unitActivityList/todo-list/detail` 回读，不能作为全客户端网络的唯一采集手段。

本轮只补研究与验收记录，没有修改 V2 代码、默认 Adapter 或发送能力，也没有重新运行代码测试。完整脱敏结论见[学生提交、教师批改与 IM 双客户端采集复核](../../../01-research/CLASSIN-ASSESSMENT-IM-DUAL-CLIENT-CAPTURE-2026-09-16.md)；原始证据保存在 Git 忽略的 `.runtime/private/homework-submit-grade-dual-capture-2026-09-16/`。

## 上一阶段记录（D-155，保留审计；不作为 D-156 完成门槛）

状态：**已交付真实读取与草稿生成切片；INT-3 真实 IM 闭环未完成，整体目标尚未完成。**

推进状态（2026-09-15）：**外部依赖阻塞**。完成CI-008j后连续三轮复核，当前代码仍明确禁用普通IM，现有Apifox文档与研究结果没有提供可执行的正式身份/群映射/发送/回读合同。没有待返回的研究任务；不通过重复测试或新增模拟结果将目标标为完成。恢复条件为平台提供受支持的普通IM接口或SDK/桥接合同；实际测验提交/批阅样本另需已验证的本人学生鉴权。详细缺口与已完成证据见下表和对应研究报告。

文档链：[PRD](./PRD.md) → [Feature Spec](./FEATURE-SPEC.md) → [Tickets](./TICKETS.md) → 本清单。实现与验证截至 2026-09-15 04:39（Asia/Shanghai）。用户授权已记录为 D-155；已按CI-008f为一名用户自有测试学生提交一份明确标记的模拟答案；按CI-005a新增一堂专用联调短课，未调整既有课堂时间或新增正式消息。

## 预览与操作

V2 测试入口：[打开测试课程与 AI 消息助手](http://127.0.0.1:4174/teacher/classin-test)。首次打开选择“老师视角”后会回到这个入口。普通 `/teacher/messages` 继续为原 Demo，页面“返回演示”有明确区分。

左栏逐单元显示全部活动和时间，中栏点击读取真实详情，右侧复用 AI 消息助手。建议回归：

1. 检查班级、目标数学课程、14 个主题单元和 54 项活动（新增一堂独立联调短课，原53项保留）。
2. 点第一讲在线课堂，核对实际时长 2,713 秒、应到 3/实到 0、4 条教师笔记、高光计数36、板书计数2。继续查看“课堂回放”：专用接口有1个文件、1707秒，实际录制19:46:49–20:15:14。点击“读取课堂回放”，使用播放器播放/拖动进度，最后“关闭课堂回放”；真实Chrome已验证播放及跳到第10分钟继续播放。
3. 点第一讲作业，检查正文、两份图片资源、三名分配学生与状态；点“读取图片”查看真实题图。当前2人已交、1人未交。展开“查看已提交内容”：原样本保留文字和1张答题图片，新样本为四题的模拟文字解答；均待批阅。原答题图片已可通过“读取答题图片”显示，并可“打开答题原图”放大；AI尚未识别图中答案。列表批阅统计与学生明细有差异，页面明确提示；不能据列表数声称已全部批阅。
4. 点第一讲测验，展开 10 道真实题目及教师参考答案/解析。第9、10题可点“读取题干图片”，查看后可“打开题干原图”或关闭；7份测验14张原图均已核对。图片显示不等于AI已识别，答案选择码按接口原值保留。在每个学生下展开“查看测验逐题作答”，当前每人10题均未参与，无实际成绩。
5. 对 AI 说“下一节在线课堂是什么时间”“第1讲作业谁还没交”“第1讲测验第2题的解析”。还可问“比较第7讲与第9讲测验的第2题”，AI会按需补查另一讲。也可点“整理任务提醒”；新增短课位于第1讲，右侧“课前”可点“整理课前提醒”，核对9月15日（周二）19:30–19:45后编辑或取消。
6. 点任一学习资料，展开“查看 PDF 文字层”，或打开/下载原PDF；14份均已实际读取。点录播后“读取并播放视频”，4段均已验证解码和开始播放，时长约5.5–5.8分钟。可问AI“第1讲资料的知识点”；当前图片识别和视频转写未接入。
7. 确认发送按钮显示“发送未接通”且禁用。此入口尚不能把消息发进 ClassIn 原生群。
8. 打开某项作业详情后点“刷新真实数据”，确认详情也重新读取。编辑中的AI消息草稿会保留；若详情更新失败，应显示独立错误，而不是继续展示旧答案。刷新时可以切换其他活动，较早返回的结果不会覆盖新选择。

本机进程已启用测试连接。重启完整 V2：在仓库根目录执行 `CLASSIN_TEST_ENABLED=1 npm run dev -- --host 127.0.0.1 --port 4174 --strictPort`。若 Harness 3080 已在运行，仅启动 UI：`CLASSIN_TEST_ENABLED=1 npm run dev:ui -- --host 127.0.0.1 --port 4174 --strictPort`。凭据沿用 `~/.classin.token`，不复制到前端或 `.env`。

## C01–C20 核验与反馈

### 产品功能盘点（2026-09-15 07:05实时回读与当前代码核对）

核心已完成的是授权单教师/单班级/单课程的“真实数据读取→AI按需查询→生成、编辑和审阅草稿”。IM是主要交付阻塞，但不是唯一剩余项。当前接入位于独立`/teacher/classin-test`；普通`/teacher/messages`及其他Demo页面没有整体替换成真实业务。

| 功能 | 当前完成范围 | 尚未完成或验证边界 |
| --- | --- | --- |
| 身份、课程目录、成员 | 测试教师鉴权；1班、1课程、14主题单元、3学生；54活动归属/时间/发布状态读取 | 多教师、多班多课程通用接入、真实多页活动数据未验收 |
| 课堂与教学动态 | 15课堂（原14讲＋联调短课）；真实时钟的课前窗口、作业/测验任务提醒 | 实时缺勤/全勤、已发布课堂改期写回与对应发送复核未完成 |
| AI查询与草稿 | 同源业务Context、受限按需读取、跨讲次查询、真实模型草稿、审阅/编辑/复制/取消 | 无ClassIn原生聊天历史；学生私聊个性化服务入口未接入；自然语言回答仍需审阅 |
| 作业 | 14份正文/附件/分配/状态；正式提交文字和答图；一学生真实提交后AI待交名单同步 | 只验证单份新增模拟答案；首讲当前2已交待批阅/1未交；批阅统计差异仍须原生核对，完整批改写回未实现 |
| 测验 | 7份试卷、每份10题；多题型、教师答案解析、14张题干图、逐题状态 | 当前三学生均未参与；真实开始/保存/交卷/批阅及实际分数正例未跑通，学生图片答卷未接入 |
| 录播与资料 | 4视频真实解码/播放；14份一页PDF读取/打开/下载及文字进入AI Context | 观看完成事件的真实变化未验收；无视频转写；PDF文字提取不等同公式/版式完整还原 |
| 课堂回放 | 首讲1个实际文件，1707秒；播放、跳转第10分钟、关闭通过 | 不代表15堂课均有回放，也不代表覆盖完整排课时段；未转写回放内容 |
| 课后数据 | 报告汇总、应到/实到/迟到、实际时长、教师本人笔记、高光/板书数量 | AI授课分析只查有无记录，未接完整分析正文；高光/板书只计数，未接原图/片段；课后出勤不能代替实时出勤 |
| 媒体理解 | 教师可查看作业题图、答图和测验题图 | 显示图片不等于AI视觉识别；当前真实数据链路未接图片理解和视频转写 |
| IM交付 | 明确显示发送未接通，不产生虚假回执 | 身份/群映射、发消息、历史回读、学生收件、持久幂等、重启恢复均待正式合同与实现 |
| 稳定性 | 输入/草稿保留、详情刷新、失败重试、选择竞态、权限/用途/会话隔离、耗时日志 | 真实发送后的故障恢复未验收；没有生产部署、多用户鉴权完成结论 |

本次实时回读确认：活动为15课堂/14作业/7测验/4录播/14资料；首讲报告实际2713秒、应到3/实到0、高光36/板书2，AI分析记录状态存在，回放1文件。此处“状态存在”仅代表check接口hasRecord，不代表分析正文已经读取。全仓最近有效验证仍为971项Vitest、29项Playwright和类型/Lint/构建通过，本次盘点未重复运行测试或修改业务数据。

“通过”只指表内已列出的范围；部分通过、未验证、待外部依赖不计入整体完成。

| 编号 | 检查内容 | 本次结果与证据 | 用户反馈 |
| --- | --- | --- | --- |
| C01 | 测试身份、机构及权限 | 通过：服务端单教师签名；浏览器身份覆盖请求 403；本机同源限制 | |
| C02 | 班级→课程→单元→活动归属 | 通过：真实 1 班、目标数学课程、14主题＋1空默认单元、54活动；逐层ID核对；仅第一讲增加联调短课 | |
| C03 | 班级成员与活动分配分开 | 通过：独立成员接口3学生；五类活动各自学生列表；身份/成绩不按数组位置关联 | |
| C04 | 分页和状态完整性 | 通过当前单页范围：单元数量逐项核对；多页/缺页/重复明确失败；多页协议未验证 | |
| C05 | 真实时钟与课前窗口 | 通过当前案例：9月15日19:30–19:45专用短课进入真实24小时窗口，正确课前事项与模型草稿；原无课分支、取消/隐藏/边界契约仍保留 | |
| C06 | 顶部事项与AI同源 | 通过：均从ClassInTestService读取，快照版本及来源可追溯；读取过程中改动会要求重读 | |
| C07 | 缺失、过期、无事件不造事实 | 通过：实到0与AI分析分开；无成绩保持null；错误保留输入且无Mock回退 | |
| C08 | 自由查询和快捷入口 | 通过当前限定范围：支持当前课程、讲次/活动类型/具体题号与快捷动作；已接入受限模型查询工具；真实页面跨第7/9讲测验查询通过 | |
| C09 | 草稿事实正确 | 通过真实canary：任务提醒截止2026-09-16 19:30；短课提醒2026-09-15星期二19:30–19:45，与API及历法一致（已修复首稿星期错误） | |
| C10 | 审阅编辑、取消、未确认不发送 | 通过当前只读阶段：编辑/复制/取消可用；真实发送禁用，未产生ClassIn消息 | |
| C11 | 业务变化后的发送复核 | 部分：focusRefs/query保留、版本冲突检测；CI-005c已核对当前编辑合同，已发布课堂改期及分配保留方式待确认，本轮未修改；真实发送复核仍依赖INT-3 | |
| C12 | 正式消息回读与三学生收件 | 待外部依赖：未获得普通IM正式HTTP/SDK/原生桥接合同，不能用课堂聊天或推送替代 | |
| C13 | 重试/重启/未知结果幂等 | 未完成：需要正式IM接入能力及持久回执；未用Mock测试冒充真实通过 | |
| C14 | 学生提交变化驱动催交名单 | 通过单样本：CI-008f真实学生接口提交后由1已交/2未交变2已交/1未交；学生、教师、V2页面与真实模型集合一致；原样本保留。明确为模拟作答，未批阅 | |
| C15 | 测验题型及作答评分 | 部分：10题含多题型、答案解析与学生状态已读；7份测验逐题状态已读取，每份3人×10题均未参与；实际已作答/批阅、图片答案仍待验证 | |
| C16 | 课后报告、录播及资料 | 部分：正式报告/教师笔记/高光计数/AI记录状态已接入；首讲回放1文件/1707秒，V2已通过实际播放/进度跳转/关闭验收；14份PDF实际文字读取、原附件打开/下载、作业图片显示和4段录播播放通过；学生答题图片和14张测验题干图已支持教师查看；图片识别和视频转写仍未接入 | |
| C17 | 个人信息按用途控制 | 契约通过：班级草稿Context剔除个人成绩和标准答案；正式交付范围仍需INT-3核验 | |
| C18 | 跨课程/环境/会话隔离 | 通过当前范围：未知活动、其他身份403；独立classin-test运行scope和teacher/tenant/thread绑定；普通会话、过期、停止、取消或换轮均拒绝工具读取；旧物理Demo消息回归通过 | |
| C19 | 性能与错误可定位 | 通过当前读取/生成：BFF含requestId、elapsedMs；受限本机日志记录路径/耗时/结果，无凭据与原文 | |
| C20 | 刷新、恢复与回退 | 部分：刷新同时重读已选详情，输入与编辑草稿保留；现场失败标记旧数据，详情失败/活动移出范围不展示旧结果；选择竞态通过；演示模式独立。真实发送重启回读尚未完成 | |

## 自动化与实际联调证据

| 验证 | 结果 |
| --- | --- |
| TypeScript / 本次Write Set ESLint | 通过 |
| 构建 `npm run build` | 通过；既有大Bundle提示仍在 |
| 首轮全量Vitest | 137文件/864项通过 |
| 最终全量 `npm run test` | 147文件/971项通过，38.93秒；默认4 workers |
| 研发中发现的失败与修复 | 图片渲染曾30秒超时，日志确认PNG约1秒已产生，等待浏览器退出拖延；增加自有渲染进程2秒关闭上限及定向终止，6项渲染/清理测试通过。构建、默认高并发Vitest和E2E同时运行曾有11项旧UI失败；默认改4 workers，串行回归880项通过。未增加测试超时 |
| 新入口＋原消息助手浏览器回归 | 最终29项通过，49.3秒，含题干图片按需读取/权限失败/重试/关闭、详情刷新/失败/活动移除/选择竞态、答题图片、课堂回放、历史图片归属、消息草稿、会话恢复及旧Demo；刷新学生状态断言补充后单项复测通过 |
| 入口补充回归 | 新入口5项通过，含首访角色恢复、刷新保留输入、680px/1440px、axe无违规 |
| 全仓 `npm run lint` | 最终通过：CommonJS文件按Node/commonjs语法检查并允许require；删除未使用变量，修正两份脚本的浏览器求值与等价Unicode写法；保留其余规则。未执行造数上传 |
| 真实API与真实浏览器 | 原53活动＋1专用短课，当前54活动、三学生、五类详情、10测验题、首讲报告与笔记均实测；学生提交详情约1.3秒且浏览器展开通过。最后现场刷新863ms；浏览器无console error/横向溢出 |
| 真实媒体读取 | 14份PDF均1页、文字424–518字符；首讲实际PDF进入Context（共4945字符）；作业PNG浏览器宽1200px；4段MP4均1280×720、时长330.8–346.2秒，在Chrome解码并开始播放。未全程观看或转写视频 |
| Harness只读工具测试 | 21项通过；包含普通/停止/取消/过期/换轮拒绝、身份与用途不可覆盖、受限响应、符号链接拒绝；实际Harness启动和模型调用另行验证 |
| 真实模型查询 | 旧大Context首次约13.5秒出现首段，最终回答第2讲，2026-09-16 15:30–17:30，与API一致；首段时间不当完整耗时 |
| 真实提醒生成 | CI-005原canary约8.8秒；CI-009独立测试scope复测生成→审阅21.739秒，用途message-draft，截止9月16日19:30、1已交/2未交及作业20–25分钟要求均与API一致；发送禁用，取消恢复 |

研发中发现完整课程来源元数据会触发Runtime 12,000字符上限；已按Spec改为完整目录/计数＋明确的按需明细窗口，Context最大7,800字符，超限要求细化问题，未静默截断事实。典型Context：下一课约3,396字符、首讲作业约4,453字符、指定题解析约4,147字符。

## 证据位置与外部依赖

- [LMS接口重测与字段口径](../../../01-research/CLASSIN-TEST-INTEGRATION-PROBE-2026-09-14.md)
- [普通IM接入依赖](../../../01-research/CLASSIN-IM-INTEGRATION-PROBE-2026-09-14.md)
- [附件下载合同与原生客户端证据](../../../01-research/CLASSIN-RESOURCE-DOWNLOAD-RESOLUTION-2026-09-15.md)
- [正式报告与AI分析冲突](../../../01-research/CLASSIN-INT4-REPORT-REPROBE-2026-09-15.md)
- [已发布课堂改期合同门槛](../../../01-research/CLASSIN-PUBLISHED-CLASS-RESCHEDULE-CONTRACT-2026-09-15.md)：编辑接口存在，不等于已验证可改已发布课堂；本轮仅读取，54项活动与短课原时段保持。
- [测验题干图片读取](../../../01-research/CLASSIN-EXAM-IMAGE-READ-CONTRACT-2026-09-15.md)：7份试卷14张原图与V2字节一致，题源0→2的已验证对应关系与首次失败均留证。
- 本机受限证据：`.runtime/private/classin-integration-int0-2026-09-14/`；截图、实际模型文本和原始接口响应不进入公共报告。运行读取日志：`.runtime/private/classin-test/reads.jsonl`，无body/key/token。

INT-3需要平台提供普通IM正式发送/历史接口或支持的客户端桥接/SDK，以及测试教师IM认证、班级群类型/ID映射、回执和幂等语义。获得合同后继续CI-006/007；当前不承诺真实群消息已经发送或学生已经收到。

当前构建/单元测试/浏览器验证按顺序执行，避免同时启动多个重负载套件。CI-010a 的渲染回归先构造永不完成的正常关闭替身，确认失败，再实现2秒关闭与仅终止自有BrowserServer；没有停止用户的Chrome进程。原公式溢出修复和两类PNG样例保持通过。

CI-008d新增资源服务/PDF解析测试和活动归属拒绝测试；实际媒体证据在受限目录browser/resources-final.{png,json}、all-resource-details.json、all-recordings-playback.json。浏览器仅获得本机附件路由，不包含上游资源地址或票据。Node要求≥22.13.0；PDF文字排版/公式请对照原PDF，不把文字抽取等同版式完全还原。

## CI-009a 实际模型查询证据

已增加独立classin-test会话与read_classin_context只读工具。每轮读取权限由服务端重验并绑定用途；普通Demo会话不获得权限，模型不能通过工具参数更改身份或升级用途。首次使用新版本测试入口会建立独立会话，不自动把以前的Demo运行会话迁入新权限。

真实页面问题“比较第7讲与第9讲测验的第2题”：前端提供第7讲快照，模型主动调用一次工具取得第9讲第2题，工具无错误，运行18.053秒完成。第7讲答案x=3，第9讲答案3或7，均与对应题库接口核对一致；浏览器无pageerror和页面级横向溢出。证据：受限browser/ci009-ui-{run,result,history,verification}.json及ci009-ui.png。首次服务端canary曾因题序标签不充分而补查全卷，现已保留题序；不把该重复读取视为必要步骤。

联调发现Harness原始输出Schema要求结构声明且不支持maxLength等关键字；已按当前rc2规则声明结构，长度约束保留在执行器。新增HTTP权限反例最初被Fetch重写Host，改为原始HTTP请求后，确认恶意Host实际返回403；鉴权失效返回401。该修正没有放宽服务端权限规则。

CI-009a已完成：最终npm run check为143文件/907测试、Harness21测试、Playwright19测试，构建通过。新scope草稿审阅证据在browser/ci009-draft.{json,png}；本次没有提交学生作业或向ClassIn群发送消息。普通IM依赖、学生提交变化正例及真实课前24小时窗口正例仍保留未完成。


## CI-008e 逐题状态与分数语义

严格按PRD R3/R5/R6 → Spec §11 → Ticket CI-008e实施。当前一手前端明确教师pageRole=1；Apifox3470597与实际errno=1相互印证。7份真实测验逐份核对bizId、分配学生、题目ID/题源/题型，全部每份3名学生、每人10条记录；所有isAnswer=2，没有真实学生作答或评分事件。预建studentExamId及score=0均不呈现为实际成绩。教师逐题折叠面板已接入，私有查询按需取得；群草稿排除个人答卷。

真实首讲完整Service读取2.518秒、BFF1.709秒；浏览器展开三名学生后核对30条“未参与”，无“得分：0”、pageerror或页面级横向溢出。受限证据：exam-answer-role/service-detail.json、bff-detail.json、all-exam-verification.json、browser-verification.json及browser.png。研究来源见[角色合同](../../../01-research/CLASSIN-EXAM-ANSWER-ROLE-CONTRACT-2026-09-15.md)。未调用提交或批阅写接口。

新增7项合同验证覆盖未参与占位、待批/实际零分换算、越权题目/学生/题源、缺失重复、媒体地址隔离与用途过滤。非零评分由契约替身验证，尚无真实正例；综合题和对象结构答案明确不可用。最终npm run check通过（144文件/914项，43.62秒），构建通过，20项Playwright通过（46.5秒）。Harness未改动，沿用CI-009a的21项验证。当时保留C05真实课前窗口、C12/13普通IM、C14新提交变化、C15已作答及图片语义等未完成项；C14后续由CI-008f完成单样本验证。


## CI-008f 学生提交变化闭环

PRD → Spec §12 → Ticket CI-008f已落盘后，仅为一名此前未交的自有测试学生提交首讲四题的模拟文字答案。第一次raw票据/省略可选标记计数的请求返回通用104，停止并双端确认无提交；按当前文档字段及此前成功格式显式恢复一次，Bearer及三个零标记计数的组合返回errno=1。原因未通过变量隔离验证，不把它写成确定的鉴权或缺字段根因。

当前真实状态为2人已交待批阅、1人未交，原已审阅样本逐字段未变。学生端正文完全匹配，教师端同一记录可读，V2实机页面和真实AI（11.345秒）均正确呈现新名单。没有调整日期、批改或发送群消息。单次accepted提交留有ProposedAction、既有用户授权、双端权限/状态校验、两次attempt各自记录和最终ExecutionReceipt。

[完整联调记录](../../../01-research/CLASSIN-STUDENT-SUBMISSION-CHANGE-2026-09-15.md)。受限证据在student-submission-change/。验收脚本默认不提交，需显式execute；已执行attempt再运行会在网络前拒绝，已核验。产品代码本轮未变，沿用914项Vitest、20项Playwright与构建基线；本次业务成功以实际API/UI/模型证据证明。

整体仍未完成：普通IM发送/回读/三学生收件依赖外部合同；测验真实作答/批阅、图片内容理解及其余完整里程碑验收仍按清单继续推进。


## CI-005a / CI-005b 真实课前窗口和日历事实

[联调报告](../../../01-research/CLASSIN-UPCOMING-CLASS-VERIFICATION-2026-09-15.md)。仅新增9月15日19:30–19:45独立短课，现54活动/15课堂（含1测试短课）；原53项逐字段不变，三学生及其他单元不变。真实动态快捷入口、实际模型草稿、编辑/取消通过，零普通IM发送。

首份模型错误补写周三，已保留失败证据；经CI-005b提供Asia/Shanghai计算的星期后，同入口重测21.209秒，正确输出星期二及19:30–19:45。新的跨UTC日历合同先红后绿。此为当前案例回归，不是所有自然语言日期的自动校验保证，仍需教师审阅。

测验生命周期的[新鲜文档与前端研究](../../../01-research/CLASSIN-STUDENT-EXAM-LIFECYCLE-CONTRACT-2026-09-15.md)已对应开始、保存、交卷和教师批阅，但合法学生签名接入仍缺少证明；作业票据不能直接外推到测验。本轮未创建测验作答或批阅事件。

最终回归：npm run check通过（144文件/915项Vitest，42.49秒；类型与Lint通过），构建通过（既有大Bundle提示），20项Playwright通过（48.3秒）。915相比914新增日历合同；Harness未变，沿用21项合同基线。

## CI-008g 专用课堂回放补齐

[当前合同和实测](../../../01-research/CLASSIN-REPLAY-METADATA-CONTRACT-2026-09-15.md)。此前仅看webLiveReplayUrl的读取缺口已修复：首讲专用接口返回1文件，1707秒，9月14日19:46:49–20:15:14录制；新短课成功返回空列表。错误响应与空列表分开，回放URL/凭据不出服务端。

UI与AI均读取上述真实结果；针对纯回放问题排除未与时间轴对齐的教师笔记，避免从笔记推测开头讲授内容。修复后模型20.098秒正确回答文件/时长/起止，并明确实际开讲时刻及缺失时段具体内容未知。课堂回放在V2只展示元数据，实际播放仍用ClassIn客户端；独立录播课播放已通过是另一项能力。未改写课节或录制数据。

CI-008g最终回归：npm run check通过145文件/923项Vitest（41.89秒）、类型与全仓Lint；构建通过；21项Playwright（35.2秒）通过。未改Harness，沿用21项合同基线。真实回放浏览器最终复核无pageerror/横向溢出，截图ui-final.png。全部验证仅为读取和生成，未新增业务写入。

## CI-008h 真实课堂回放播放

PRD R5/R6 → Spec §15 → Ticket CI-008h先落盘后实现。课堂回放采用单独的服务端流Interface，保持旧20MiB教学附件边界。每次请求重读当前教师/课程/课堂/文件权限，浏览器仅提交活动与版本引用；使用32字节MP4探测、强ETag及精确Range合同，服务端逐块传送，不完整缓冲163MB回放。关闭、超时或长度异常终止请求，不把JSON错误混入媒体流。

真实Chrome实测：5,255ms就绪，1920×1080/1707.066秒；开始播放到1秒，跳到600秒后继续播放至601秒，无media error；关闭后video移除。界面无pageerror/横向溢出，截图人工核对。实际BFF32字节探测206且ftyp正确；越界Range返回416及正确总长，身份覆盖/失效引用/未知课堂均403。未整片观看，未改变录制设置或产生学生观看/作答事件。

19项流合同测试覆盖200/206/416、版本/长度/类型/编码、非MP4、截短与超量、背压、中止、响应头超时及输出后失败关闭；授权与活动归属反例另由replay/service测试覆盖。浏览器自动化验证失败、重试与加载中关闭；真实解码及seek以实际媒体证据为准。一次测试断言将Uint8Array与Buffer直接比较导致失败，统一字节表示后通过，没有放宽业务校验。

最终 `npm run check`：类型、全仓Lint、146文件/944项Vitest通过（43.21秒）；构建通过（既有Bundle大小提示）；22项Playwright通过（约60秒）。Harness未修改，沿用21项基线。证据在受限replay-stream/及check/build/playwright-ci008h-final.log；[合同与详细验收](../../../01-research/CLASSIN-REPLAY-STREAM-CONTRACT-2026-09-15.md)。本票完成限定播放能力，INT-3普通IM、测验真实学生鉴权等依赖仍未完成。

## CI-008i 已提交答题图片

严格按PRD R3/R5/R6 → Spec §16 → Ticket CI-008i推进，先重核五份Apifox定义及一张真实答图，再实施。教师图片读取重新核对当前成员、作业/单元/bizId、分配名单、已正式提交状态、提交记录ID、四归属和精确图片关系；不从题干附件列表中取学生答案。当前只支持isDownload=1及两处filePermission=0的已验证条件，其他状态明确拒绝；不把未知权限枚举猜成允许。

真实V2首讲答卷图片返回200/image/jpeg、114216字节，1705ms，900×1234；SHA-256与独立API下载原图完全一致。“打开答题原图”新页可显示900px原图，关闭后预览移除；截图人工核对，无pageerror/横向溢出。换为另一未交学生、另一已交学生、失效引用和UID覆盖实际均403。只读取现有答卷，当前仍2人已交待批阅、1人未交，没有代交或批阅。

实测发现提交fileSize112与关系fileSize111均非精确字节数，已先修Spec；复用实际流计数的20MiB限制，不做错误等值判断。图片版本引用及供应商地址不进入模型Context，群草稿不含个人提交；模型尚未识别图片内容，不据此生成正确率或评分。兼容旧响应只有附件计数的情况。

最终 `npm run check`通过146文件/949项Vitest（41.06秒）、类型与全仓Lint；构建通过（既有Bundle提示）；23项Playwright通过（约66秒），覆盖答图按需加载、失败/重试、原图入口和关闭。真实图片原图新页及权限反例另有实机证据。Harness未修改，沿用21项合同基线。

[读取合同与实测](../../../01-research/CLASSIN-SUBMISSION-IMAGE-READ-CONTRACT-2026-09-15.md)。私有证据submission-image/v2-verification.json、v2-answer.png及check/build/playwright-ci008i-final.log。INT-3普通IM合同、测验学生合法鉴权及实际答题评分仍未完成；本票不将C16或整体目标标为全部完成。

## CI-010c 刷新已选活动详情

按PRD R1/R5/R6 → Spec §17 → Ticket CI-010c修复。原父页面只更新scene，子页面独立保存detail，没有将刷新完成结果与已选活动关联。浏览器最小用例先失败：点击刷新后4秒仍无新详情；轨迹包含scene/catalog/dynamics各3次（含初始化），detail仅1次，证明未重新请求详情，而非上游缓存或旧响应覆盖。

修复后先刷新现场，再重读当前选择；即使scene版本相同也读取详情，因为评语/答卷等明细变化未必改变目录版本。现场失败保留先前成功详情并明确旧数据；详情失败和活动离开范围移除旧结果。选择序号使较早请求不能覆盖随后选中的活动。没有增加轮询或新的API，没有改动学生答案。

新增5项浏览器回归覆盖详情内容与学生状态更新、现场失败、详情失败重试、活动移除和延迟响应竞态，原失败用例先红后绿；学生状态由未提交变为已提交待批阅的断言另行复测通过。原输入保留合同继续通过。

真实页面生成催交草稿12.018秒，进入编辑后改写文案，再点刷新；详情读取时间由04:06:08更新到04:06:22，内容版本不变，仍2人已交待批阅/1人未交。编辑文字保留，发送禁用，取消成功，无pageerror/横向溢出，截图人工核对。此为真实只读刷新与真实模型/编辑验证；实际提交变化沿用CI-008f证据，本轮不新增学习事件。

最终949项Vitest（40.78秒）、类型/Lint、构建及28项Playwright（47.7秒）通过；Harness未改，沿用21项基线。证据：private/refresh-consistency/verification.json、model-result.json、refreshed.png、student-status-regression.log及check/build/playwright-ci010c-final.log。此修复关闭了手动刷新详情的实现缺口，普通IM交付/重启回读、学生测验鉴权仍未完成。


## CI-008j 测验题干原图

PRD R3/R5/R6 → Spec §19 → Ticket CI-008j。首次真实联调因查询与返回topicSource不相等导致没有题图引用；重新核对7卷70题，全部为查询0→返回2，按已验证组合修正，未知组合仍拒绝。失败证据保留，不能把单元替身成功当真实接通。

14张实际题图均HTTP200、PNG、1200×760，总792858字节，与各自题库引用原图逐字节一致。读取中位1193ms、最大1457ms。真实Chrome首讲第9题显示、打开原图新页、关闭均通过，无pageerror/页面溢出；未知/其他活动、未知题目、旧引用、身份与URL覆盖6项实际403。图片只供教师预览，Context保留文字不完整状态，不含图片引用，不声称AI已识别。

最终全仓类型/Lint/147文件971项Vitest（38.93秒）、构建、29项Playwright（49.3秒）通过；Harness无修改，沿用21项合同证据。媒体原始响应、逐图hash及截图位于受限private/exam-images/。没有新建题目、作答、批阅或正式IM消息。
